import { ActivitiesPipe } from './activities.pipe';

describe('ActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
