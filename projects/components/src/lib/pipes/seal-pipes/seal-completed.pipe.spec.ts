import { SealCompletedPipe } from './seal-completed.pipe';

describe('SealCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new SealCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
