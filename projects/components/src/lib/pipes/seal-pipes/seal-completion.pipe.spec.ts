import { SealCompletionPipe } from './seal-completion.pipe';

describe('SealCompletionPipe', () => {
  it('create an instance', () => {
    const pipe = new SealCompletionPipe();
    expect(pipe).toBeTruthy();
  });
});
