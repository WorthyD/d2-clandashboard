import { SealGuildedPipe } from './seal-guilded.pipe';

describe('SealGuildedPipe', () => {
  it('create an instance', () => {
    const pipe = new SealGuildedPipe();
    expect(pipe).toBeTruthy();
  });
});
