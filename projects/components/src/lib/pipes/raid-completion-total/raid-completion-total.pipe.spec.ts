import { RaidCompletionTotalPipe } from './raid-completion-total.pipe';

describe('RaidCompletionTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new RaidCompletionTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
