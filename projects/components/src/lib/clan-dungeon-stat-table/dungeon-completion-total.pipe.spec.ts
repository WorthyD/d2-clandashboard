import { DungeonCompletionTotalPipe } from './dungeon-completion-total.pipe';

describe('DungeonCompletionTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new DungeonCompletionTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
