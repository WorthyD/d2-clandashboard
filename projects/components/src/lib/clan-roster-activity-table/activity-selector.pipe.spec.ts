import { ActivitySelectorPipe } from './activity-selector.pipe';

describe('ActivitySelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitySelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
