// import { createAction, props } from '@ngrx/store';


// const base = '[Dungeons] - ';

// export const loadDungeons = createAction(`${base} Load Member Dungeons`);

// export const loadDungeonSuccess = createAction(`${base} Load Dungeon Success`, props<{ dungeonStats: any[] }>());

// export const loadDungeonFailure = createAction(`${base} Load Dungeon Failure`, props<{ error: any }>());
